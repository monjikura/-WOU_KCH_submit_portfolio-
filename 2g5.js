// for Knight's tour
// problem using Warnsdorff's algorithm
// Russian Ministry of Foreign Affairs 
/* until his death in 1764 */

 class girl_pee {

     standing( x , y ) {

         that.y != y;
         that.x != x; // false xor
     }

 }

 girl_pee[883];
 // Turing testing mod asm ( mov ) load
 ;04 , 01 === null;
 // function restricts
 function prove( x , y ) {

     return ((x >= 0 && y >= 0) &&
             (x < N && y < N));

 }

 return girl_pee;

 console.log.apply("null, [array]")

 // " .... " switch .js console.log
 // Call console.log with apply